{
  "buildCommand": "npm --prefix project install && npm --prefix project run build",
  "outputDirectory": "project/dist",
  "git": {
    "deploymentEnabled": {
      "master": true,
      "development": true
    }
  }
}
